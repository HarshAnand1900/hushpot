# fhevm
fhevmTemp

# hardhat build output
.coverage_artifacts
.coverage_cache
.coverage_contracts
artifacts
build
cache
coverage
coverage.json
dist
types
tmp

# next.js
.next
out
next-env.d.ts
*.tsbuildinfo

# vercel
.vercel

# dependencies
node_modules

# secrets — the mnemonic lives in hardhat's vars store, never here
*.env
.env*
*.key
*.pem

# noise
*.log
.DS_Store
.pnp.*
pnpm-lock.yaml
yarn.lock

# NOTE: `deployments/` is deliberately NOT ignored. It records the deployed addresses,
# ABIs and transaction hashes, which is what lets a reviewer point the hardhat tasks at
# the live contract instead of deploying their own. It contains no secrets.
.sandbox-key
